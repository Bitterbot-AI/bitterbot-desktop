# Tauri build output
target/
gen/

# SEA sidecar binaries (produced by scripts/build-sea.mjs per platform)
binaries/
.sea-work/

# Native module addons copied in by build-sea.mjs
resources/*.node

# macOS build artifacts
*.dmg
*.app

# Icons generated by `pnpm tauri icon` — keep the source PNG, ignore
# the rest since they're reproducible.
# (Delete this block if you want the generated icons committed.)
