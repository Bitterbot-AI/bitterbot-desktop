---
summary: "CLI reference for `bitterbot uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `bitterbot uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
bitterbot uninstall
bitterbot uninstall --all --yes
bitterbot uninstall --dry-run
```
